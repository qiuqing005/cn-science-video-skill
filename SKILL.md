---
name: cn-science-video
description: "把用户提交的中文科普剧本制作成成片；自动并行生成普通话解说、检索并下载高关联公共素材，必要时自主选择可信 TTS 模型，再完成字幕、HyperFrames 渲染和验收。不用于产品宣传、网站录制或只加字幕。"
---

# 中文科普视频制作

将用户给出的剧本制作成可发布的中文科普视频。默认使用 HyperFrames HTML 渲染，默认横屏 1920x1080、普通话男性声线、沉稳统一语气、最终 1.2 倍速。收到完整剧本后直接启动制作，不要求用户再次确认已经明确的参数。

执行前读取 [references/workflow.md](references/workflow.md)。需要寻找素材时读取 [references/public-media.md](references/public-media.md)；现有 TTS 不满足要求或用户要求换模型时读取 [references/tts-models.md](references/tts-models.md)。

## 执行原则

- 默认直接制作，不先输出冗长方案。只有用户明确要求方案、素材授权不清、或缺少必须的源视频时才暂停询问。
- 默认采用“快速创作模式”：一次性确定标题、受众、时长、画幅、视觉路线和声音参数；不重复读取无关文档或反复改稿。
- 剧本分段并落盘后，必须同时启动两条独立工作流：`voice` 负责 TTS、响度和时间戳，`assets` 负责关键词扩展、公共素材检索、许可核查、下载和转码。两者运行时，主流程继续搭建 storyboard、原创图解和项目结构。不要等一条完成后才开始另一条。
- 并行任务写入边界固定：`voice` 只写 `audio/`、`audio_meta.json` 和词级时间戳；`assets` 只写 `assets/source/`、`assets/stock-render/` 和 `assets/manifest.jsonl`；主流程负责脚本、storyboard、HTML 和最终集成。
- 用户文案是事实来源，但必须修正明显的绝对化、过时数字和医学误导。无法核实的统计数据改成审慎表述，必要时在画面或片尾加“仅供科普，不能替代医疗建议”。
- AI 应自主把每段旁白扩展成中英文检索词并寻找公共素材库，不要求用户提供网址。视觉优先选择与旁白语义直接相关且许可明确的视频、图像和图表；找不到合适素材时使用原创动态图解、粒子、器官示意或数据可视化。不要用无关风景填充时长。
- 每个外部素材必须冻结到项目目录并登记来源 URL、作者、许可、下载时间和使用段落。不能确认许可时不使用；不得把搜索结果页、临时 CDN URL 或仅供预览的水印文件当成交付素材。
- 解说默认使用现有本地 Qwen3-TTS 1.7B 男声 `Uncle_Fu`，指令固定为标准普通话、沉稳、自然、克制、段落能量一致。除非用户指定，不使用方言音色或情绪大幅起伏的提示词。
- 现有模型不能满足语言、音色或稳定性时，AI 可以自主从 Hugging Face、ModelScope 或项目官方仓库评估并下载 TTS 模型。先核对许可证、维护状态、模型大小、12GB 显存适配和推理依赖；优先官方权重和 safetensors，拒绝不明网盘、任意可执行安装包与未审查的远程代码。
- 所有下载的依赖、模型、缓存、媒体和输出必须位于当前分配目录下，默认 `H:\短剧`。运行前检查 `TEMP`、模型缓存和下载目录；不要把新依赖装到 C 盘。发现本任务新建且落在 C 盘的缓存或依赖时，先迁移到 `H:\短剧`，再删除对应的孤立副本；不要删除系统或其他项目文件。
- 1.2 倍速只在最终音画合成阶段执行：视频 `setpts=PTS/1.2`，音频 `atempo=1.2`，并使用保持音高的处理。不要让各镜头分别变速导致字幕和转场漂移。
- GPU 可用时优先 H.264 NVENC；必须保留 CPU fallback。不要为了“使用 GPU”牺牲清晰度、音频同步或验收。

## 必须的交付门禁

1. 先建立项目目录和 `hyperframes.json`，保存用户原始剧本，记录默认参数、依赖位置与授权假设。
2. 形成机器可读的分段清单后，在同一轮工具调用中启动 `voice` 和 `assets`；日志分别写入项目目录，失败互不覆盖。
3. 两条工作流都结束后再合流；以真实音频时长同步时间线，以素材清单绑定镜头。单条失败时只重试失败条目一次，并用已有模型或原创图解降级，不重跑全部任务。
4. 检查普通话、断句、音量、语气一致性、素材相关性和许可记录，然后执行 `lint`、`validate`、`inspect` 及中点快照/接触表。
5. 最终检查分辨率、帧率、时长、音画同步、音频响度和编码；确认 `renders/final.mp4` 存在后才报告完成。

## 路由边界

- 只有原创图形和文字、没有真实素材时，可改用 `faceless-explainer`。
- 用户提供口播或访谈原片并要求删口误、保留人物时，用 `cut-talking-head` / `finish-talking-head`。
- 用户只要求在已有视频上加字幕，用 `embedded-captions`。
- 任何视频编辑或渲染仍先读取 `hyperframes`；不要绕过 HyperFrames 自行猜测项目结构。
